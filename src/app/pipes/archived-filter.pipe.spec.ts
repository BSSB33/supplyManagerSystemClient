import { ArchivedFilterPipe } from './archived-filter.pipe';

describe('ArchivedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArchivedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
