import { OrderBuyerFilterPipe } from './order-buyer-filter.pipe';

describe('OrderBuyerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBuyerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
