import { CompanyNameFilterPipe } from './company-name-filter.pipe';

describe('CompanyNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
