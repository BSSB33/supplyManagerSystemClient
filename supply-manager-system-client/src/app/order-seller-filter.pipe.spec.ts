import { OrderSellerFilterPipe } from './order-seller-filter.pipe';

describe('OrderSellerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSellerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
